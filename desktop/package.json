{
  "name": "desktop",
  "version": "0.1.3",
  "private": true,
  "scripts": {
    "dev": "npx tauri dev",
    "build": "npx tauri build",
    "build:debug": "npx tauri build --debug",
    "sidecar:build": "node scripts/build-sidecar.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
