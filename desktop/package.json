{
  "name": "desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/ensure-rust.mjs tauri dev",
    "build": "node scripts/ensure-rust.mjs tauri build",
    "build:debug": "node scripts/ensure-rust.mjs tauri build --debug",
    "sidecar:build": "node scripts/build-sidecar.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
